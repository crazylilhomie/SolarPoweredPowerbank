# Solar Powerbank Survey Dashboard

This repository contains a complete Streamlit dashboard for analyzing solar powerbank market survey data and performing user purchase prediction using machine learning.

## 🚀 Features
- Upload dataset (CSV/XLSX)
- 10 interactive visualizations useful for business decisions
- Classification models:
  - Random Forest
  - Decision Tree
  - Gradient Boosting
- Predict whether a new customer will buy the product
- Download prediction results as CSV
- Ready for deployment on GitHub + Streamlit Cloud

## 📁 Files Included
- `app.py` — Main Streamlit application
- `visuals.py` — Visualization scripts
- `model_utils.py` — ML preprocessing and prediction logic
- `requirements.txt` — Dependencies list
- `README.md` — Project documentation

## ▶️ Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## 📤 Deployment
You can directly upload this project folder to GitHub and deploy on Streamlit Cloud.
